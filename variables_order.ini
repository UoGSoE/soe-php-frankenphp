; docker-injected env vars must land in $_ENV so laravel's env() helper can
; see them whatever php.ini the SAPI ends up with - GPCS (no E) is a common
; distro/production default
variables_order = EGPCS
